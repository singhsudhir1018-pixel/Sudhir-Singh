rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // परीक्षणको लागि: लगइन भएका सबै प्रयोगकर्तालाई Read/Write अनुमति दिने
      allow read, write: if request.auth != null;
    }
  }
}